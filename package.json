{
  "name": "apple-health-atlas",
  "version": "0.1.0",
  "private": true,
  "description": "A privacy-first, browser-only visual explorer for Apple Health CSV exports.",
  "type": "module",
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-privacy.mjs",
    "start": "python3 -m http.server 4173 --directory web"
  },
  "dependencies": {
    "echarts": "^6.1.0",
    "jszip": "3.10.1"
  },
  "engines": {
    "node": ">=20"
  }
}
